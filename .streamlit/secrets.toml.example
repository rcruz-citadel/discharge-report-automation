# Copy this file to `.streamlit/secrets.toml` and update the values.
# Do not commit real credentials to source control.

DATABASE_URL = "postgresql://username:password@hostname:5432/database"

# ── Microsoft OAuth (Azure AD) ──────────────────────────────────────────────
# Register an app at https://portal.azure.com → Azure Active Directory →
# App registrations → New registration.
#   - Supported account types: "Accounts in this organizational directory only
#     (Single tenant)" — Citadel Health and Aylo Health share the same tenant.
#   - Redirect URI: Web → http://YOUR_SERVER:8501
# Then: Certificates & secrets → New client secret (copy the Value, not ID)
# API permissions → Microsoft Graph → Delegated → User.Read → Grant admin consent

AUTH_CLIENT_ID     = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
AUTH_CLIENT_SECRET = "your-client-secret-value"
AUTH_TENANT_ID     = "342ecc4e-69ba-4b09-a7e9-c14d0b2ff0f0"

# Comma-separated list of allowed email domains (leave empty to allow all)
AUTH_ALLOWED_DOMAINS = "citadelhealth.com,aylohealth.com"

# Must exactly match the Redirect URI registered in Azure
AUTH_REDIRECT_URI = "http://localhost:8501"
